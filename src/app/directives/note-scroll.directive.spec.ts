import { NoteScrollDirective } from './note-scroll.directive';

describe('NoteScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new NoteScrollDirective();
    expect(directive).toBeTruthy();
  });
});
